@model IEnumerable<CorParques.Negocio.Entidades.GestionMantenimientoControl>


<table id="datatable-responsive" class="table table-striped table-bordered" cellspacing="0" width="100%">
    <thead>
        <tr>
            <th>
                Atraccion
            </th>
            <th>
                Descripcion
            </th>
            <th>
                Usuario
            </th>
            <th>
                Fecha
            </th>
            <th style="width:50px"></th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null)
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Atraccion)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Descripcion)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UsuarioModicifacion)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FechaModificacion)
                    </td>
                    <td style="text-align:center; width:50px">
                        <a class="lnkEdit" data-id="@item.Id" href="javascript:void(0)"><b class="fa fa-pencil-square"></b></a>
                        @*<a class="lnkDelete" data-id="@item.Id" href="javascript:void(0)"><b class="fa fa-times-circle"></b></a>*@
                    </td>
                </tr>
            }
        }

    </tbody>
</table>