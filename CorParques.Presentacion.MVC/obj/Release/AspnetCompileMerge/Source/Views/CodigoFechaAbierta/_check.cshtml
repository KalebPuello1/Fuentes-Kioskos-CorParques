@model IEnumerable<CorParques.Negocio.Entidades.CodigoFechaAbierta>

<!--datatable-responsive-->
@section scripts{
    <script>
    function edit(e) {
            e.preventDefault();
            console.log("this is f")
        }
        function reden(e) {
            e.preventDefault();
            console.log("this is ff")
        }

    </script>}
<table id="example" class="col-md-12 col-sm-12 col-xs-12 display table table-striped table-bordered" style="width:100%">
     <thead>
        <tr>
            <th>
                Pedido
            </th>
            <th>
                Codigo barras
            </th>
            <th>
                Fecha Inicial
            </th>
            <th>
                Fecha Final
            </th>
            <th>
                Fecha redencion
            </th>

            <th style="width:50px"></th>

        </tr>
    </thead>
    <tbody id="put">
        @if (Model != null)
        {
            foreach (var item in Model)
            {
        <tr>
            <td>
                @item.CodSapPedido
            </td>
            <td>
                @item.CodBarrasBoletaControl
            </td>
            <td>
                @item.FechaInicial
            </td>
            <td>
                @item.FechaFinal
            </td>
            <td>
                @if (item.Redencion == 1)
                {
                    @item.FechaRedencion
                }
            </td>
           
                <td style="text-align:center; width:50px">
                    @*<a class="lnkDisable" data-id="@item.IdPedidoBoletaControl" href="javascript:void(0)"><b class="fa fa-mail-forward"></b></a>*@
                    <a class="lnkDisable" data-id="@item.CodBarrasBoletaControl" href="javascript:void(0)"><b class="fa fa-mail-forward"></b></a>
                    @if (item.Redencion == 0)
                    {
                        <a class="lnkEdit" data-id="@item.IdPedidoBoletaControl" href="javascript:void(0)"><b class="fa fa-eye"></b></a>
                    }
                </td>
 
        </tr>
            }
        }
    </tbody>
</table>

